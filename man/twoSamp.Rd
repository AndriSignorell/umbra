% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twoSamp.R
\name{twoSamp}
\alias{twoSamp}
\title{Generate two random samples}
\usage{
twoSamp(
  n,
  rdist,
  groups = 1:2,
  valueName = "value",
  groupName = "group",
  output = c("long", "wide")
)
}
\arguments{
\item{n}{numeric vector containing one common or two separate sample sizes.}

\item{rdist}{random-generating function or list of two random-generating
functions. Each function must accept the sample size as its first
argument and return a numeric vector of that length.}

\item{groups}{vector of length two containing the group values.}

\item{valueName}{name of the value variable in long format.}

\item{groupName}{name of the group variable in long format.}

\item{output}{output format, either \code{"long"} or \code{"wide"}.}
}
\value{
a \code{data.frame} containing the two generated samples. In wide
format, the shorter sample is padded with missing values.
}
\description{
Generates two random samples and returns them in long or wide format.
}
\examples{
set.seed(42)

d <- twoSamp(
  n = c(100, 120),
  rdist = list(
    function(n) rnorm(n, mean = 70, sd = 8),
    function(n) rnorm(n, mean = 73, sd = 9)
  ),
  groups = c("2023", "2024"),
  valueName = "gewicht",
  groupName = "jahr"
)
}
