% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rndPairs.R
\name{rndPairs}
\alias{rndPairs}
\title{Generate correlated random pairs}
\usage{
rndPairs(
  n,
  r,
  rdist1 = stats::rnorm(n = n),
  rdist2 = stats::rnorm(n = n),
  prop = NULL
)
}
\arguments{
\item{n}{number of random pairs to generate.}

\item{r}{desired correlation coefficient between \code{-1} and \code{1}.}

\item{rdist1}{numeric vector of length \code{n} containing the values of the
first random variable.}

\item{rdist2}{numeric vector of length \code{n} containing the values of the
second random variable.}

\item{prop}{\code{NULL}, a numeric vector of category proportions, or a list
containing separate proportions for the two variables. Proportions must
be positive and sum to one.}
}
\value{
a \code{data.frame} with the variables \code{x} and \code{y}. If \code{prop} is
specified, both variables are returned as quantile-based factors.
}
\description{
Generates pairs of correlated random values and optionally converts them
into categorical variables with specified proportions.
}
\details{
The second variable is calculated as

\deqn{y = r x + \sqrt{1-r^2}z,}

where \eqn{x} and \eqn{z} are given by \code{rdist1} and \code{rdist2}. If these
variables are independent, standardised random variables, the resulting
Pearson correlation is approximately \code{r}.

If \code{prop} is a numeric vector, the same category proportions are applied
to both variables. A list of length two can be used to specify different
proportions, with the first element referring to \code{x} and the second to
\code{y}.
}
\examples{
set.seed(42)
d <- rndPairs(n = 1000, r = 0.7)
cor(d$x, d$y)

set.seed(42)
d <- rndPairs(
  n = 1000,
  r = 0.5,
  prop = c(0.25, 0.50, 0.25)
)
table(d$x)
table(d$y)
}
